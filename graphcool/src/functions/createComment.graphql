subscription {
  Comment(filter: { mutation_in: [CREATED] }) {
    node {
      id
      content
      author {
        id
        username
      }
      post {
        id
        author {
          id
        }
      }
    }
  }
}
